# Firebase (optional - app runs with in-memory DB without these)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Gemini AI (optional - falls back to local analysis)
GEMINI_API_KEY=

# Server
PORT=5001
USE_MEMORY=true
